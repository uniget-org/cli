scorecard:
  image: registry.gitlab.com/uniget-org/cli:noble
  before_script: |
    uniget install scorecard
  script: |
    scorecard --repo=${CI_REPOSITORY_URL} --ref=${CI_COMMIT_SHA} --output=scorecard.json
  artifacts:
   paths:
   - scorecard.json