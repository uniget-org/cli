% docker-setup

$ tool: docker-setup list --- --column 2 --delimiter ' '

# Install default tools
sudo docker-setup install --default

# Install all tools
sudo docker-setup install --all

# Install specific tool
sudo docker-setup install <tool>